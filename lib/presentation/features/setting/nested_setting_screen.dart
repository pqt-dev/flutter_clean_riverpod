import 'package:flutter/material.dart';

class NestedSettingScreen extends StatelessWidget {
  const NestedSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Nested setting screen'),
      ),
    );
  }
}
